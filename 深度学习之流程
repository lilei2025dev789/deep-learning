今日偶有所感，特写深度学习之流程乎：
1.现在这里有很多张图片，图片有feature和label两类
2.找一个函数（非常复杂的一个函数），由卷积层，池化层，BN(批量归一化层)，VGG块，NiN小网络块，残差块等诸多部分组成
3.通过这个函数对feature进行运算得出很多的10维矩阵，每一行10个数，在那个位置的数字最大，则表示这个feature的类别最可能是什么，这里有10类物品，比如T恤，裤子等等
4.然后通过计算出来的与真实的答案进行比对，计算出他的loss，然后反向传播自动求出loss对每个参数的梯度，告诉优化器往这个方向调能让loss变小，优化器更新参数，然后根据这个差距去调整函数里的参数，这个过程经过上万次重复后，f变得越来越准
5.最后就看一下最终训练的结果的训练accuracy和loss以及测试的accuracy来判断这个网络模型的效能
